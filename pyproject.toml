[project]
name = "argus"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.51",
    "psycopg[binary]>=3.2",
    "apscheduler>=3.11",
    "pyyaml>=6.0.2",
    "python-dotenv>=1.1",
]
